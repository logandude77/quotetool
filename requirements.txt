# Core geometry (conda-forge):
#   conda install -n quotetool -c conda-forge pythonocc-core pyside6
# Display backend for OCC viewer on Windows (use pip PyQt5 with pythonocc):
PyQt5>=5.15,<6
pytest>=8,<9
